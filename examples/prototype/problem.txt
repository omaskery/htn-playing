
state {
    Time is 0,
    TimeOfDay is 10,

    Location(NPC) is Base,

    IsBrightAt(0) is false,
    IsBrightAt(1) is false,
    IsBrightAt(2) is false,
    IsBrightAt(3) is false,
    IsBrightAt(4) is false,
    IsBrightAt(5) is false,
    IsBrightAt(6) is false,
    IsBrightAt(7) is false,
    IsBrightAt(8) is true,
    IsBrightAt(9) is true,
    IsBrightAt(10) is true,
    IsBrightAt(11) is true,
    IsBrightAt(12) is true,
    IsBrightAt(13) is true,
    IsBrightAt(14) is true,
    IsBrightAt(15) is true,
    IsBrightAt(16) is true,
    IsBrightAt(17) is true,
    IsBrightAt(18) is true,
    IsBrightAt(19) is false,
    IsBrightAt(20) is false,
    IsBrightAt(21) is false,
    IsBrightAt(22) is false,
    IsBrightAt(23) is false,
    IsBrightAt(24) is false,

    TravelTimeInHours(Base, Farm) is 4,
    TravelTimeInHours(Farm, Base) is 4,
    TravelTimeInHours(Base, Camp1) is 2,
    TravelTimeInHours(Camp1, Base) is 2,
    TravelTimeInHours(Camp1, Camp2) is 2,
    TravelTimeInHours(Camp2, Camp1) is 2,
    TravelTimeInHours(Camp2, Camp3) is 2,
    TravelTimeInHours(Camp3, Camp2) is 2,
    TravelTimeInHours(Camp3, Farm) is 1,
    TravelTimeInHours(Farm, Camp3) is 1,

    CanCamp(Base) is true,
    CanCamp(Camp1) is true,
    CanCamp(Camp2) is true,
    CanCamp(Camp3) is true,
    CanCamp(Farm) is true,

    NextCampFrom(Base, Farm, 3) is Camp1,
    NextCampFrom(Camp1, Farm, 3) is Camp2,
    NextCampFrom(Camp2, Farm, 3) is Camp3,
    NextCampFrom(Farm, Base, 3) is Camp3,
    NextCampFrom(Camp3, Base, 3) is Camp2,
    NextCampFrom(Camp2, Base, 3) is Camp1,
}

goals TravelTo(NPC, Farm);
